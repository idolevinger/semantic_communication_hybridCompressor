# Virtual environment
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# macOS metadata
.DS_Store

# Large data files (too big for Git)
data/

# Trained model weights
trained_pipeline.pt
results/*.pt
*.pt

# Experiment results (plots are KEPT, but .pt files are ignored above)
# (removed results/ so .png files can be tracked)

# Stray nested working copy
files (2)/

# Jupyter notebooks checkpoints
.ipynb_checkpoints/
